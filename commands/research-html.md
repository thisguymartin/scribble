---
description: Create a sourced research report as a standalone HTML artifact
argument-hint: "[topic and sources]"
---

Use the `research-html-discovery` skill and produce a single-file HTML artifact.

Use local codebase and git by default. Web and MCP sources are opt-in. Include citations, confidence levels, and diagrams when they clarify the flow.

User request:

$ARGUMENTS
